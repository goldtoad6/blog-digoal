interface:
  display_name: "Find Postgres Bug"
  short_description: "Hunt bugs in a local PG source tree"
  default_prompt: "Use $find-postgres-bug to scan a local PG source tree, build a debug+cassert instance, pick a recent suspicious commit, write and run a .sql repro against the running instance, and produce a pgsql-bugs-style markdown report."
